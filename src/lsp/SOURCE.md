# lsp-mode port (Jemacs)

Upstream: [emacs-lsp/lsp-mode](https://github.com/emacs-lsp/lsp-mode)  
Local ELPA snapshot: `lsp-mode-20240529.2057` (version 9.0.1)

## Full protocol (`src/lsp/lsp-protocol/`)

| Emacs | Jemacs |
| --- | --- |
| `lsp-protocol.el` `lsp-interface` (213 types) | `generated/builders.ts` — `lspMake*` + `lsp*P` per interface |
| Autogenerated enum constants | `generated/enums.ts` |
| `lsp-interface` registry | `generated/registry.ts` |
| LSP 3.17 spec types | `vscode-languageserver-types` + `vscode-languageserver-protocol` (re-exported) |
| `JSONResponse`, errors | `jsonrpc.ts`, `errors.ts` |

Regenerate Emacs-derived bindings after upgrading ELPA:

```bash
bun run generate:lsp-protocol
# or: LSP_PROTOCOL_EL=/path/to/lsp-protocol.el bun run generate:lsp-protocol
```

## Transport & runtime

| Emacs | Jemacs |
| --- | --- |
| `lsp--make-message`, parser filter | `transport.ts` |
| `lsp--client`, workspace, session | `client.ts`, `workspace.ts`, `session.ts` |
| `lsp--client-capabilities` | `capabilities.ts` (typed `ClientCapabilities`) |
| Buffer sync / diagnostics / completion | `sync.ts`, `diagnostics.ts`, `completion.ts` |
| `lsp-pylsp.el` | `clients/pylsp.ts` |

## Emacs commands

`lsp`, `lsp-mode`, `lsp-shutdown-workspace`, `lsp-workspace-restart`, `lsp-describe-session`, `lsp-toggle-trace-io`
